[package]
name = "mneme_expression"
version = "0.1.0"
edition = "2021"

[dependencies]
mneme_core = { path = "../mneme_core" }
mneme_limbic = { path = "../mneme_limbic" }
rand = "0.8"
chrono = "0.4"
async-trait = "0.1"
anyhow = "1.0"
tokio = { version = "1", features = ["sync"] }
tracing = "0.1"
